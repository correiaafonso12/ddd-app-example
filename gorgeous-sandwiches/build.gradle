subprojects {

    repositories {
        mavenCentral()
    }
    
    tasks.withType(Jar) {
        def moduleName = project.path.replace(':', '-').replaceAll('^-+', '')
        archiveBaseName.set(moduleName)
    }
}
